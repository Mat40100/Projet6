{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('build/addTrickForm.css') }}" />
{% endblock %}

{% block body %}
    <section class="row justify-content-center align-items-lg-center m-0">
        {{ form_start(form) }}
        <div class="col-12 row justify-content-center m-0 p-0">
            <div class="col-8 text-center">
                {{ form_row(form.name) }}
            </div>

            <div class="col-8 text-center">
                {{ form_row(form.description) }}
            </div>

            <div class="col-12 row row justify-content-center">
                <div class="col-12 col-lg-6 row justify-content-center">
                    <div class="col-12 text-center">
                        {{ form_row(form.medias) }}
                    </div>
                    <a href="#" id="add_media" class="add-btn btn btn-outline-primary col-6 m-2">Ajouter un media</a>
                </div>
                <div class="col-12 col-lg-6 row justify-content-center">
                    <div class="col-12 text-center">
                        {{ form_row(form.videos) }}
                    </div>
                    <a href="#" id="add_videos" class="add-btn btn btn-outline-primary col-6 m-2">Ajouter une vidéo</a>
                </div>
                <div class="col-12 col-lg-6 row justify-content-center">
                    <div class="col-12 text-center">
                        {{ form_row(form.categories) }}
                    </div>
                </div>
            </div>
            <div class="col-12 row justify-content-center mb-5">
                <input type="submit" class="btn btn-outline-secondary" value="Envoyer">
            </div>
        </div>
        {{ form_end(form) }}
    </section>
{% endblock %}

{% block js %}
    <script type="text/javascript" src="{{ asset('build/form.js') }}">   </script>
{% endblock %}